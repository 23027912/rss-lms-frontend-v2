## Feed Data
Placeholder feed data is located in `lib/feedData.ts` for temporary use until backend integration is complete.

## Themes
The web portal supports two UI themes: **Light** and **Dark**. Theme selections are persisted using the browser's local storage.


